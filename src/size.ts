/**
 * Represents a size.
 * @public
 */
export interface Size {
  /**
   * The pixels that density-independent.
   */
  width: number;

  /**
   * The pixels that density-independent.
   */
  height: number;
}
